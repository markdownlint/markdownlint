# Example of a custom .mdlrc and style sheet

This directory has a sample .mdlrc file and the style sheet it calls.

It will exclude a very long line like this one that would ordinarily be flagged by mdl as a violation.
